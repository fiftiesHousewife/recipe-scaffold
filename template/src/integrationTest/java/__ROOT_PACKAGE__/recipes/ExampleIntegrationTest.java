package __ROOT_PACKAGE__.recipes;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/**
 * Placeholder integration test. Real integration tests use
 * {@code withToolingApi()} against a fixture project to verify behaviour
 * that {@code RewriteTest} can't prove (e.g. real Maven Central resolution,
 * real {@code GradleProject} markers).
 *
 * <p>Disabled because the no-op {@link ExampleRecipe} has nothing
 * meaningful to assert against an embedded daemon. Replace this class once
 * you write your first real recipe — see
 * {@code .claude/skills/recipe-testing/SKILL.md} for the canonical
 * harness shape.
 */
@Disabled("Replace ExampleRecipe with a real recipe before enabling.")
class ExampleIntegrationTest {

    @Test
    void placeholder() {
    }
}
