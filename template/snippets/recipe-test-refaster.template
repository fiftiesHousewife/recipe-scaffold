package {{package}};

import org.junit.jupiter.api.Test;
import org.openrewrite.test.RecipeSpec;
import org.openrewrite.test.RewriteTest;

import static org.openrewrite.java.Assertions.java;

// Note: this test references `{{recipeName}}Recipes` (plural), the aggregate
// Recipe generated by rewrite-templating's annotation processor. The source
// `{{recipeName}}` class is a template holder; you don't instantiate it.
class {{recipeName}}Test implements RewriteTest {

    @Override
    public void defaults(final RecipeSpec spec) {
        spec.recipe(new {{recipeName}}Recipes());
    }

    @Test
    void appliesTheTemplate() {
        rewriteRun(
                java(
                        """
                                package com.example;

                                public class Example {
                                    boolean check(boolean flag) {
                                        return flag == true;
                                    }
                                }
                                """,
                        """
                                package com.example;

                                public class Example {
                                    boolean check(boolean flag) {
                                        return flag;
                                    }
                                }
                                """
                )
        );
    }
}
